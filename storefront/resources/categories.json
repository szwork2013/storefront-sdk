{
  "url": "http://api.beta.vtex.com/:account/categories/",
  "cacheDuration": 60,
  "parameters": {
    "account": "{{ account.name }}"
  },
  "queryString": {
    "depth": "{{ parameters.depth }}"
  }
}
